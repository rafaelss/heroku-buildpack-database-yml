#!/usr/bin/env bash
BUILD_DIR=$1

ls -lsa $3/

app_env=$(cat $2/RAILS_ENV)

echo "$app_env:\n  url: <%= ENV[\"DATABASE_URL\"] %>\n  encoding: \"utf-8\"\n  schema_search_path: \"public, common, extensions\"" > $BUILD_DIR/config/database.yml.test

exit 0
